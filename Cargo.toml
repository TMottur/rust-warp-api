[package]
name = "rust-web"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.219", features = ["derive"] }
tokio = { version = "1", features = ["full"]}
warp = "0.3.7"
serde_json = "1.0"
